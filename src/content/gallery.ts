import type { GalleryItem } from "./types";

/**
 * TO COLLECT — no photographs have been supplied.
 *
 * Empty, so the gallery section does not render. Add entries pointing at real
 * files in public/images/ with accurate alt text and the section reappears;
 * the homepage grid alternates landscape and portrait automatically based on
 * each image's dimensions.
 */
export const gallery: GalleryItem[] = [];
